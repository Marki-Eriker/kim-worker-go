type PaginationOutput {
  totalItems: Int!
  totalPages: Int!
  page: Int!
  itemsPerPage: Int!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

interface CoreOutput {
  ok: Boolean!
  error: [ProblemInterface!]
}
